import { Button } from './ui/button'

const MainNavigator = () => {
  return (
    <Button
      variant={'ghost'}
      className="font-bold bg-orange-500 hover:text-orange-900 hover:bg-orange-200"
    >
      Sign in
    </Button>
  )
}

export default MainNavigator
